# G0004 实施结果

> 实施日期：2026-09-03  
> 状态：代码、文档和本地自动化门禁完成；Standalone 人工视觉与真实 Host 保存恢复待验收

## 已完成

- 增加“细节、流动、卷曲”艺术参数，直接双向映射 Julia 真实参数；
- 增加暮金花火、深海丝绒、霓虹珊瑚 3 个视觉预设和琥珀金、极光青、玫紫夜 3 个调色板；
- 增加确定性九宫格、0.05–1.00 强度、全部/只变形态/只变质感；
- 增加 Seed、构图、形态、颜色锁定；当前唯一 Julia 公式天然固定，效果明确留到 G0006；
- 增加候选采用、收藏、恢复、从候选续变和从收藏续变；
- 参数元数据声明了 ID、显示名、分组、范围、步长和分布；
- 缩略图使用最多 3 路有界并发、240 像素预算、64 项内存缓存和协作取消；
- Document 使用整批 generation 提交，取消和迟到结果不改变作品或上一批候选；
- 作品格式升级到 v3，保存强度、锁定、模式、轮次、候选配方和收藏，不保存缩略图；
- v1/v2 作品显式迁移到 v3，原有渲染参数保持不变。

## 自动化证据

- 当前 58 项测试全部通过；
- 相同作品、Seed 和轮次生成相同的 9 个候选配方；
- 全部分组锁定时每个候选都保持原配方；
- 只变质感不会改变 Seed、构图或形态；
- 所有候选重新进入统一作品验证器；
- 候选渲染实测最大并发不超过 3，重复批次 9 项全部命中缓存；
- 取消整批渲染抛出取消且 Document 不提交半批状态；
- 候选采用、撤销、收藏、恢复和保存恢复在 Document 测试中贯通；
- v2→v3 迁移、v3 探索字段往返以及运行态 Bitmap 隔离均有测试。

## 待人工验收

- 在 Standalone 检查右侧属性区滚动、不同窗口尺寸和 DPI 下九宫格横向滚动是否自然；
- 观察快速连续生成、取消、收藏高亮和高精度缩略图反馈；
- 在真实 Host 验证 v2 文件升级、v3 保存关闭恢复、Dirty 提示和 Document Scope 释放；
- 确认视觉预设和调色板具有足够区分度。

本轮按要求未使用 AIFLOW，未增加 Windows CI，未执行部署、发布或正式 ZIP 门禁。
